protectionError:
  type: object
  description: JSON is invalid and one or more potential threats were detected.
  required:
  - type
  properties:
    type:
        type: string
        format: uri
    title:
        type: string
    status:
        type: integer
    detail:
        type: string
    instance:
        type: string
        format: uri